# 🎬 Demonstration: Launching the Employee Directory Application

## 🧠 Big Picture (Before Details)

What are we doing?

> We are launching a **web application** on
> **Amazon EC2**,
> inside a **default network**,
> with **secure permissions**,
> and **automatic app installation**.

This is **classic real-world AWS**.

![Image](https://res.cloudinary.com/hy4kyit2a/f_auto%2Cfl_lossy%2Cq_70/learn/modules/aws-compute/review-the-amazon-ec2-instance-lifecycle-and-pricing/images/5439d6d3d960459631bc7c3268c0e2ae_kix.z857a0xyssny.png)

![Image](https://d2908q01vomqb2.cloudfront.net/22d200f8670dbdb3e253a90eee5098477c95c23d/2018/01/21/AR_Diagram_010418.png)

![Image](https://docs.aws.amazon.com/images/vpc/latest/userguide/images/how-it-works.png)

---

## 🖥️ Step 1: Launch an EC2 Instance

From the AWS Console:

* Go to **EC2 Dashboard**
* Click **Launch instance**
* Name it: `employee-directory-app`

📌 Naming doesn’t affect functionality, but it **massively helps** in real projects.

---

## 🧱 Step 2: Choose an AMI (OS Template)

### What was chosen?

👉 **Amazon Linux 2023**

This is:

* AWS-managed
* Secure
* Optimized for EC2
* Long-term supported

🧠 Remember:

> **AMI = OS + base software configuration**

You *could*:

* Use Marketplace AMIs
* Use vendor AMIs
* Create your **own golden AMI** for companies

---

## ⚙️ Step 3: Choose Instance Type

Chosen:
👉 **t2.micro** (free-tier eligible)

Why?

* Cheap
* Enough for demo workloads
* General-purpose

🧠 Instance type decides:

* CPU
* RAM
* Network performance

---

## 🔐 Step 4: Key Pair (Skipped on Purpose)

They selected:
👉 **Proceed without key pair**

Why this is OK:

* Not SSH’ing manually
* Using **Console Connect**
* Viewing logs, not admin shell access

⚠️ In production:

* You’d usually use key pairs or SSM

---

## 🌐 Step 5: Network Settings (Default VPC)

Chosen:

* **Default VPC**
* **Default subnet**
* **Auto-assign public IP = Enabled**

What this means:

* Instance gets a **public IP**
* Internet access works
* Easy for demos & learning

🧠 Default VPC already has:

* Internet Gateway
* Public subnets

Later you’ll learn:
👉 Private subnets + Load Balancers (real-world setups)

---

## 🔥 Step 6: Security Group (Firewall)

Security groups are:

> **Instance-level firewalls**

Configured rules:

* ❌ SSH (port 22) → blocked
* ✅ HTTP (port 80) → allowed
* ✅ HTTPS (port 443) → allowed

🧠 Why this matters:

* Only necessary traffic is allowed
* Reduces attack surface

---

## 💾 Step 7: Storage

* Default root volume only
* No extra EBS volumes

Simple, clean, minimal 👍

---

## 🛂 Step 8: IAM Role (CRITICAL STEP)

Selected:
👉 **EmployeeWebAppRole**

This is HUGE 🔥

This role allows:

* Access to **Amazon S3**
* Access to **Amazon DynamoDB**

🧠 Why this is best practice:

* ❌ No hardcoded credentials
* ✅ Temporary credentials
* ✅ Auto-rotated
* ✅ Secure by design

> EC2 → assumes role → gets credentials → calls AWS APIs

---

## ⚡ Step 9: User Data (Auto Setup Script)

This is **cloud magic** ✨

User data = script that runs **ON FIRST BOOT**

### What the script does:

1. Declares Bash
2. Downloads app ZIP from S3
3. Unzips application
4. Installs Python + pip
5. Installs Flask dependencies
6. Installs `stress` (for future autoscaling demos)
7. Sets environment variables:

   * S3 bucket (placeholder)
   * AWS region (Oregon)
   * DynamoDB mode ON
8. Runs the Flask app on **port 80**

🧠 This is called:

> **Bootstrapping an EC2 instance**

---

## 🚀 Step 10: Launch & Wait

After clicking **Launch**:

* Instance enters **Running**
* Status checks initialize
* You WAIT ⏳

Only after:
✔ System check
✔ Instance check

…is it ready.

---

## 🌍 Step 11: Access the Application

* Copy **public IP**
* Paste into browser
* 🎉 App loads!

What you see:

* Employee Directory UI
* Empty data (expected!)

Why empty?

* S3 bucket not created yet
* DynamoDB table not created yet

That’s coming next 😉

---

## 🧠 Final Architecture (Right Now)

```
Browser
   ↓
Public IP
   ↓
EC2 (Flask App)
   ↓
IAM Role
   ↓
S3 (photos)   DynamoDB (data)  ← coming soon
```

---

## ✅ Key Takeaways (VERY IMPORTANT)

✔ EC2 launches from an AMI
✔ Instance type controls hardware
✔ Default VPC allows internet access
✔ Security groups = firewalls
✔ IAM roles = secure AWS access
✔ User data automates setup
✔ Status checks must pass
✔ Empty app ≠ broken app

---

## 🚀 What’s Next?

👉 **Next lesson:**

### 🐳 *Container Services on AWS*

(ECS, EKS, and why VMs aren’t always the answer)
